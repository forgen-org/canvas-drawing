/*
use web_sys::CanvasRenderingContext2d;

pub struct ShapeContext {
    pub min_x: u32,
    pub max_x: u32,
    pub min_y: u32,
    pub max_y: u32,
    pub min_width: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub max_height: u32,
    pub cursor_pos: Option<(u32, u32)>,
    pub cursor_movement: Option<(u32, u32)>
}

pub trait Shape {
    fn new(context: ShapeContext) -> Self;
    fn animate(self, context: ShapeContext) -> Self;
    fn draw(self, context: CanvasRenderingContext2d) -> ();
}
*/
